<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Network access for packet capture -->
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>

  <!--
    Frida requires these three to work under Hardened Runtime.
    Without them, Frida cannot inject into other processes.
    NOTE: These entitlements may be rejected by Apple notarisation
    for App Store distribution. For direct/developer distribution
    only (which is what this tool targets).
  -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- JIT for Frida's instrumentation engine -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Read process info (ps, proc_info) -->
  <key>com.apple.security.temporary-exception.mach-lookup.global-name</key>
  <array>
    <string>com.apple.system-task-ports</string>
  </array>
</dict>
</plist>
